package levels;

class Theatre extends PlayState {
    override public function create() {
        super.create();
    }

    override public function update(elapsed:Float) {
        super.update(elapsed);
    }
}